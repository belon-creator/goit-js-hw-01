'use strict';

const name = 'Генератор защитного поля';
let price = 1000;
price = 2000;
console.log('Выбран', name, 'цена за штуку', price);

// const message = `Выбран ${name} цена за штуку ${price}`;
// console.log(message);
